import type { ThemeDefinition } from "@/lib/theme/types";

/**
 * Modern urban premium — cool surfaces, teal brand, crisp CTAs.
 * Wire into `ProjectSlug` + `PROJECT_REGISTRY` when the Godrej project ships.
 */
export const godrejKukatpallyTheme: ThemeDefinition = {
  id: "godrej-kukatpally",
  viewportThemeColor: "#f4f6f8",
  typography: {
    displayFamily: "Fraunces",
    sansFamily: "DM Sans",
  },
};
