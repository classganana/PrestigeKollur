import type { ThemeDefinition } from "@/lib/theme/types";

/**
 * Golden Doors — rich gold brand hub.
 * Warm ivory surfaces, espresso shell, metallic gold accents.
 * Typography: Cormorant Garamond (display) + Plus Jakarta Sans (sans).
 */
export const goldenDoorsTheme: ThemeDefinition = {
  id: "golden-doors",
  viewportThemeColor: "#FAF7F0",
  typography: {
    displayFamily: "Cormorant Garamond",
    sansFamily: "Plus Jakarta Sans",
  },
};
